
; ledger {{{

; (add-to-list 'auto-mode-alist '("\\.ledger\\'" . ledger-mode))
; (add-to-list 'load-path "~/.emacs.d/ledger")

; (load-file "~/.emacs.d/ledger/ledger-mode.el")

; (add-hook 'ledger-mode-hook 'auto-revert-mode)
; (add-hook 'ledger-mode-hook 'org-link-minor-mode)

; }}}

